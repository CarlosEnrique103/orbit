<%= link_to gravatar_for(current_user), user_path(current_user) %>
<h1><%= current_user.name %></h1>
<span><%= link_to "View my profile", user_path(current_user) %></span>
<span><%= pluralize(current_user.snapshots.count, 'snapshot') %></span>
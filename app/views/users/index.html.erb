<% provide(:title, 'All users') %>
<h1>Users</h1>

<ul class="list-users">
  <% @users.each do |user| %>
  <li>
    <%= gravatar_for user %>
    <%= link_to user.name, user_path(user) %>
  </li>
  <% end %>
</ul>